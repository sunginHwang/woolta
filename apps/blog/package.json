{
  "name": "blog",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run --passWithNoTests",
    "storybook": "storybook dev -p 4400 -c .storybook",
    "build-storybook": "storybook build -o ../../dist/storybook/blog -c .storybook",
    "test-storybook": "pnpm -w exec vitest run --project storybook-blog"
  },
  "dependencies": {
    "@woolta/common": "workspace:*",
    "@woolta/wds": "workspace:*"
  }
}
