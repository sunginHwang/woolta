/** 가계부 상세·저장·삭제 — 데이터 계층은 libs 가 소유한다(woolta-api GraphQL). */
export type { AccountBookDetail } from '@woolta/woolbank-features';
export { getAccountBookFetchInfo, useAccountBookDetail } from '@woolta/woolbank-features';
