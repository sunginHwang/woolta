# Next 앱 공통 프록시 설정 — 각 vhost 에서 include 한다.
#
#   include /etc/nginx/snippets/woolta-next.conf;
#
# 주의: Next 는 자기 앞에 프록시가 있다는 걸 X-Forwarded-* 로만 안다.
# 이게 없으면 앱이 만드는 절대 URL 이 http:// 로 새어나간다.

proxy_http_version 1.1;
proxy_set_header Upgrade            $http_upgrade;
proxy_set_header Connection         "upgrade";
proxy_set_header Host               $host;
proxy_set_header X-Real-IP          $remote_addr;
proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto  $scheme;
proxy_set_header X-Forwarded-Host   $host;

proxy_read_timeout  60s;
proxy_send_timeout  60s;

# 이미지 업로드가 이 경로로 나간다(/api/gql → woolta-api). 기본 1m 이면 사진 한 장에 413 이 난다.
client_max_body_size 20m;
